name="dylan"
password="root"

# Set to either "podman" or "docker"
container_manager="podman"

container_name="container_name"
terminal_name="terminal-name"
mount_onto=$(dirname "$(pwd)")
docker_work_directory=/home/$name/"$(basename "$mount_onto")"

# Custom docker run commands
custom_args=""